const HOME = '[HOME]'
const UPDATE_SCROLL_PERCENT = `${HOME} Update Scroll Percent`
const UPDATE_SCROLL_PERCENT_OFFSET = `${HOME} Update Scroll Percent Offset`
const UPDATE_CURRENT_PROJECT_INDEX = `${HOME} Update Current Project Index`

export default {
  UPDATE_SCROLL_PERCENT,
  UPDATE_SCROLL_PERCENT_OFFSET,
  UPDATE_CURRENT_PROJECT_INDEX
}
