const normal = {
  strokeStart  : 0,
  strokeEnd    : 100,
  radius       : 30,
  opacity      : 0,
  // rgb          : { r: 0, g: 0, b: 0 },
  arrowOffset  : 30,
  arrowOpacity : 0
}

const link = {
  radius  : 0,
  opacity : 0
}

const drag = {
  radius       : 15,
  arrowOpacity : 1,
  arrowOffset  : 5
}

const sticky = {
  radius: 50
}

const loading = {
  strokeStart: 100
}

export default {
  normal,
  link,
  drag,
  sticky,
  loading
}
