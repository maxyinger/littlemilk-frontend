const CURSOR = '[Cursor]'
const EXIT_TRANSITION = `${CURSOR} Exit Transition`
const ENTER_TRANSITION = `${CURSOR} Enter Transition`
const IS_STICKY = `${CURSOR} Sticky`
const NO_CURSOR = `${CURSOR} No Cursor`
const DEFAULT = `${CURSOR} Default`

export default {
  EXIT_TRANSITION,
  ENTER_TRANSITION,
  IS_STICKY,
  NO_CURSOR,
  DEFAULT
}
