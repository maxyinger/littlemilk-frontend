const APP = '[App]'
const MAKE_STICKY = `${APP} Make Sticky`
const BREAK_STICKY = `${APP} Break Sticky`
const START_TRANSITION = `${APP} Start Transition`
const START_EXIT_TRANSITION = `${APP} Start Exit Transition`
const START_ENTER_TRANSITION = `${APP} Start Enter Transition`
const END_TRANSITION = `${APP} End Transition`
const MAKE_THEME_DARK = `${APP} Make Theme Dark`
const MAKE_THEME_LIGHT = `${APP} Make Theme Light`
const TOGGLE_CURSOR = `${APP} Toggle Cursor`

export default {
  MAKE_STICKY,
  BREAK_STICKY,
  START_TRANSITION,
  START_EXIT_TRANSITION,
  START_ENTER_TRANSITION,
  END_TRANSITION,
  MAKE_THEME_DARK,
  MAKE_THEME_LIGHT,
  TOGGLE_CURSOR
}
