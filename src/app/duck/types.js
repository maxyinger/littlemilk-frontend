const APP = '[App]'
const MAKE_STICKY = `${APP} Make Sticky`
const BREAK_STICKY = `${APP} Break Sticky`
const START_TRANSITION = `${APP} Start Transition`
const END_TRANSITION = `${APP} End Transition`
const MAKE_THEME_DARK = `${APP} Make Theme Dark`
const MAKE_THEME_LIGHT = `${APP} Make Theme Light`

export default {
  MAKE_STICKY,
  BREAK_STICKY,
  START_TRANSITION,
  END_TRANSITION,
  MAKE_THEME_DARK,
  MAKE_THEME_LIGHT
}
